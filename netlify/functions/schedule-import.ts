import { fetchEvidenceUrl } from '../../lib/ingestion/fetch-source.js';import { parseScheduleHtml } from '../../lib/ingestion/schedule/sidearm.js';import { preserveSource } from '../../db/repositories/sources.js';import { upsertScheduleEvidence } from '../../db/repositories/schedule.js';import { requireProgramContext } from './_shared/auth.js';import { json,message,statusFor } from './_shared/http.js';
export default async(request:Request)=>{try{const{user,program}=await requireProgramContext(request);const body=await request.json() as Record<string,unknown>;if(typeof body.sourceUrl!=='string')return json({error:'Schedule URL is required.'},400);const fetched=await fetchEvidenceUrl(body.sourceUrl);const source=await preserveSource({programId:program.programId,bytes:fetched.bytes,sourceFamily:'official_schedule',sourceUrl:fetched.url,contentType:fetched.contentType,importedBy:user.email,parserVersion:'schedule-sidearm-1.1.0'});const matches=parseScheduleHtml(new TextDecoder().decode(fetched.bytes),fetched.url,program.seasonYear);if(!matches.length)return json({error:'No supported schedule matches were found. The source snapshot was preserved for adapter review.',sourceArtifactId:source.id},422);const summary=await upsertScheduleEvidence({programId:program.programId,seasonId:program.seasonId,ourTeamId:program.teamId,matches,actorEmail:user.email,sourceArtifactId:source.id});return json({summary,sourceArtifactId:source.id,duplicateSource:source.duplicate});}catch(e){const m=message(e,'Schedule import failed.');return json({error:m},statusFor(m));}}
