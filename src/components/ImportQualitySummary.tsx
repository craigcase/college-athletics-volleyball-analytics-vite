import { Link } from 'react-router-dom';
import type { ImportQualitySummaryData } from '../lib/types';
export function ImportQualitySummary({quality,matchId}:{quality:ImportQualitySummaryData;matchId?:string}){
  return <section className="import-quality-summary"><div><span className="eyebrow">Import complete</span><h3>Evidence quality checked</h3></div><div className="quality-grid"><div><strong>{quality.dataLevel}</strong><span>Data level</span></div><div><strong>{quality.rallyCount}</strong><span>rallies processed</span></div><div><strong>{quality.reconciledCount}</strong><span>uniquely reconciled</span></div><div><strong>{quality.unresolvedCount}</strong><span>unresolved details</span></div></div>{quality.structuralConflictCount?<p className="quality-warning">{quality.structuralConflictCount} set-level score conflict{quality.structuralConflictCount===1?'':'s'} remain. Affected rally analytics are withheld.</p>:quality.unresolvedCount?<p className="muted">Unresolved details were excluded only from the statistics that require them.</p>:<p className="muted">No unresolved statistical details were found in this import.</p>}{matchId?<Link className="secondary-link" to={`/matches/${matchId}#data-quality`}>View Data Quality</Link>:null}</section>;
}
